{
  "name": "@costlens/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone costlens CLI — query per-feature (git-branch) cost from the shared ledger, and batch-ingest Claude Code (ccusage) sessions into it. No pi/opencode runtime dependency; just `costlens feature <branch>`.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "costlens": "./src/index.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test src/lib.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "echo \"cli: no build (TypeScript loaded by Bun)\"",
    "clean": "rm -rf node_modules .tsbuildinfo"
  },
  "dependencies": {
    "@costlens/core": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "bun-types": "^1.3.14",
    "typescript": "^5.6.0"
  }
}

/**
 * Pricing confidence — pi adapter shim.
 *
 * Phase 9 step 2: re-exports `computePricingConfidence` from
 * `@costlens/core/pricing`. Tool-agnostic; lives in core.
 */

export { computePricingConfidence, type PricingConfidence } from "@costlens/core";

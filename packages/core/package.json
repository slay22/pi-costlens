{
  "name": "@costlens/core",
  "version": "0.0.0",
  "private": true,
  "description": "Tool-agnostic cost layer. SQLite schema, lifecycle (close/cancel/merge/reopen/cap/tags/notes), search/export, dashboard server. Used by every tool adapter (pi, opencode, claude-code). Step 2 of MULTI-TOOL.md will extract the data plane from pi-costlens into this package.",
  "type": "module",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --import tsx --test src/index.test.ts src/db.test.ts src/lifecycle.test.ts src/pricing.test.ts src/config.test.ts",
    "test:server": "bun test src/server/api.test.ts src/lifecycle-server.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "echo \"core: no build (TypeScript-only, loaded via tsx/jiti)\"",
    "clean": "rm -rf node_modules .tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "bun-types": "^1.3.14",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}

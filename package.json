{
  "name": "costlens-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Costlens monorepo — a tool-agnostic cost layer. Today: a pi extension. Tomorrow: opencode and Claude Code. The shared data plane lives in @costlens/core.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "pnpm -r test",
    "test:server": "pnpm --filter pi-costlens test:server",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "bun-types": "^1.3.14",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@11.10.0"
}
